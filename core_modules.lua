local hyper = require("core.hyper.hyper")
local reload_hammerspoon = require("core.reload_hammerspoon.reload_hammerspoon")

local core_modules = {
	hyper=hyper,
	reload_hammerspoon=reload_hammerspoon,
}

return core_modules
